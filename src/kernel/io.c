#include "io.h"

// t_cmd_input current = NULL;


void add_to_input () {
    // if (current != NULL) 
    //     current = (t_cmd_input) malloc(sizeof(t_cmd_input));
    
    // current.command_line = (char)malloc(2);
    // current.command_line[0] = 'c';
    // current.command_line[1] = '\0';
    // print(current.command_line);
}